# Tested with Python 3.14.7 on Windows 11; pinned so a fresh install reproduces the models bit for bit.
pandas==3.0.6
numpy==2.5.3
lightgbm==4.7.0
joblib==1.6.0
requests==2.34.2
streamlit==1.64.0
plotly==7.1.0
